import deploymentInfo from '../../deployment-info.json';
import contractABI from '../../build/DraculaProtocol.abi.json';

export const CONTRACT_ADDRESS = deploymentInfo.contractAddress;
export const CONTRACT_ABI = contractABI;
